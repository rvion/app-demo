# appli de demo

server
client
base de donne

```
docker run \
    -it \
    --rm \
    --net=host \
    -v /home/rvion/dev/app-web:/app \
    --workdir /app \
    node \
    bash
```

----


javascript
cote client: rien 
cote server: node

------------
node
npm: node package manager